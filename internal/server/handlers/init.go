package handlers

// This file ensures all handler init() functions are executed when the package is imported.
// Without this, the Go compiler may optimize away unused handler files in release builds.
