# Octopus Domain Language

Octopus 聚合多个 LLM 上游渠道，通过分组路由或显式渠道直连完成请求转发。

## Language

**分组路由**：
客户端模型名精确命中同名分组后，由分组配置选择渠道和实际上游模型。
_Avoid_: 普通直连、默认模型

**指定渠道直连**：
客户端使用 `channelName/modelName` 请求某个已配置渠道及其缓存模型，且只允许一次目标渠道尝试。
_Avoid_: passthrough、渠道模型分组

**协议直通**：
入站与出站协议一致且无需通用请求改写时，保留原始请求和响应形态的转发优化；它不改变路由方式。
_Avoid_: 指定渠道直连、direct route

**请求模型**：
客户端提交的完整模型标识；指定渠道直连时包含渠道前缀。
_Avoid_: 实际模型

**实际模型**：
最终发送给上游渠道的模型标识；指定渠道直连时不包含渠道前缀。
_Avoid_: 请求模型

**请求选项**：
转发到上游前应用的参数覆盖与自定义请求头。分组路由中先应用分组请求选项，再由渠道请求选项覆盖同名顶层参数或同名请求头；指定渠道直连只应用渠道请求选项。
_Avoid_: 高级选项、请求改写配置
